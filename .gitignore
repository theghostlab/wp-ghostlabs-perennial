# A reviewer runs `pnpm install` in this repository to check that the published
# sources produce the published bundles. What that installs is not part of the
# distribution and must never be committed here.
node_modules/

# Composer's runtime autoloader IS published — the plugin autoloads through it —
# so vendor/ is deliberately NOT ignored. Only the dev tooling is kept out, and
# the build already does that with `composer install --no-dev`.

.DS_Store
